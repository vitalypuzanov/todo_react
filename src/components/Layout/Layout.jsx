import React from 'react';

const Layout = ({ children }) => {
  return (
    <div
      className="py-10
   bg-gray-800 h-screen text-white ц"
    >
      {children}
    </div>
  );
};

export default Layout;
